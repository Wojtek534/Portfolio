var Typed = require ('typed.js')

let right = () => {
    let typeRight = new Typed('.type-right', {
        strings: [text],
        typeSpeed: 40,
        loop: false,
        startDelay: 100
    });
}